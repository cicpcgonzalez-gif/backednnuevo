## Ejemplo de .env para el backend
# Copia este archivo a `.env` y configura con tus valores reales
# IMPORTANTE: Nunca commitear el archivo .env (está en .gitignore)

# === CONFIGURACIÓN DE BASE DE DATOS ===
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Para desarrollo local:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/rifas_db"
# Para Render (copiar "External Database URL" del dashboard):
DATABASE_URL="postgresql://amegarifasapp:rifasadmin123@DB_HOST:5432/DB_NAME"

# === SEGURIDAD ===
# Generar un JWT_SECRET único y seguro para producción
# Ejemplo: openssl rand -base64 64
JWT_SECRET="tu_jwt_secret_aqui_cambiar_en_produccion"

# === SERVIDOR ===
# Puerto donde correrá el servidor (opcional, por defecto 3000)
PORT=3000

# Modo de entorno (development o production)
NODE_ENV=development

# === CONFIGURACIÓN SMTP (Opcional) ===
# También se puede configurar desde la API con el superadmin
# Ejemplos de configuración para diferentes proveedores:

# Gmail:
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER="tu_email@gmail.com"
# SMTP_PASS="tu_app_password"

# SendGrid:
# SMTP_HOST="smtp.sendgrid.net"
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER="apikey"
# SMTP_PASS="tu_sendgrid_api_key"

# Mailgun:
# SMTP_HOST="smtp.mailgun.org"
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER="postmaster@tu-dominio.mailgun.org"
# SMTP_PASS="tu_mailgun_password"

# === NOTAS IMPORTANTES ===
# 1. Si no configuras SMTP, el sistema usará Ethereal (mock) para desarrollo
# 2. En producción, configura estas variables en el dashboard de tu hosting (Render, Heroku, etc.)
# 3. Para Gmail, necesitas crear una "App Password" desde la configuración de seguridad
# 4. El superadmin puede configurar SMTP desde la API en /superadmin/settings/smtp
